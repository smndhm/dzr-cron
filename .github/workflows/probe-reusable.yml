# Throwaway, see probe-direct.yml. Same schedule, but reaching its steps the
# way every cron does, through a reusable workflow.
name: Probe reusable

on:
  schedule:
    - cron: '2-59/5 * * * *'
  workflow_dispatch:

jobs:
  probe:
    uses: ./.github/workflows/probe-called.yml
