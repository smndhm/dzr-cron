name: Cron thibaut

on:
  # Every hour, see family-playlist for the staggering.
  schedule:
    - cron: '37 * * * *'
  workflow_dispatch:

# Several crons read a playlist another one writes, so they never overlap
concurrency:
  group: dzr-crons
  cancel-in-progress: false

jobs:
  run:
    uses: ./.github/workflows/crons.yml
    with:
      names: thibaut
    secrets: inherit
